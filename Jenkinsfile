pipeline {
  agent any

  stages {
    stage('Build') {
      steps {
        sh 'echo "Oi Jenkins"'
        sh '''
          echo "Nossa primeira pipeline"
          ls -la
        '''
      }
    }
  }
}