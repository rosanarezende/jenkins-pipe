pipeline {
  agent any

  stages {
    stage('Build') {
      steps {
        sh 'echo "Oi Jenkins... nosso sábado está excelente"'
        sh '''
          echo "Essa é nossa primeira pipeline"
          ls -la
        '''
      }
    }
  }
}
